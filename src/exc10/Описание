             Применение статических методов в интерфейсе.
      У интерфейсов появилась еще одна возможность: определять в нем один или
несколько статических методов. Аналогично статиче­ ским методам в классе, метод,
объявляемый как static в интерфейсе, можно вы­ зывать независимо от любого объекта.
И для этого не требуется ни реализация такого метода в интерфейсе, ни экземляра
самого интерфейса. Напротив, для вы­ зова статического метода достаточно указать
имя интерфейса и через точку имя самого метода. Ниже приведена общая форма вызова
статического метода из ин­ терфейса. Обратите внимание на ее сходство с формой вызова
статического метода из класса.

имя_интерфейса.имя_статического_метода

    Как упоминалось выше, для вызова метода getDefaultNumber() реализация или экземпляр
интерфейса MyIF не требуется, поскольку это статический метод. И последнее замечание:
статические методы из интерфейсов не наследуются ни реализующими их классами, ни подчи-
ненными интерфейсами.